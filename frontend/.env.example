# Base URL of the Django backend API.
#
# Production: leave EMPTY (or unset). The frontend and backend share one domain
# and nginx routes /api/ to the backend, so requests are same-origin.
#
# Local development (two separate dev servers): point at the Django dev server.
VITE_API_URL=http://127.0.0.1:8000
